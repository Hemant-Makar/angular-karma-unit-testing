export class Calculator {

    public add(first: number, second: number): number {
        return first + second;
    }

    public sub(first: number, second: number): number {
        return first - second;
    }
}
